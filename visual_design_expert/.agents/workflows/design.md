---
name: design
description: 讀取 input/prompt.txt 並一鍵生圖與輸出設計成品
---

# /design

請套用 `visual_design_expert` Skill，讀取 `input/prompt.txt` 進行圖像分析與 Prompt 合成，呼叫 `generate_image` 生圖工具將成品輸出至 `output/result.png`。

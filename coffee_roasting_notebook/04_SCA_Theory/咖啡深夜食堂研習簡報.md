---
type: sca_theory
title: "咖啡深夜食堂研習簡報"
date: 2026-01-22
tags: [coffee/sca_theory, imported/takeout]
---

# 📚 SCA 考官理論：咖啡深夜食堂研習簡報

## 📋 對話理論紀錄
```react
import React, { useState, useEffect } from 'react';
import { 
  Coffee, 
  Beaker, 
  Flame, 
  BookOpen, 
  ChevronRight, 
  ChevronLeft, 
  Atom, 
  Thermometer, 
  Wind, 
  Microscope,
  Cpu,
  Menu
} from 'lucide-react';

const Presentation = () => {
  const [currentSlide, setCurrentSlide] = useState(0);
  const [isRevealed, setIsRevealed] = useState(false);

  const slides = [
    {
      id: 'intro',
      type: 'title',
      title: '咖啡深夜食堂',
      subtitle: '豆種、製程與化學分子的感官地圖',
      author: '理化與自然科跨領域研習',
      icon: <Coffee size={64} className="text-amber-500" />
    },
    {
      id: 'menu',
      type: 'list',
      title: '本日特供：科學與感官的菜單',
      content: [
        { text: '這裡沒有玄學，只有科學。', icon: <Atom /> },
        { text: '莫札特 = 酯化反應特調 (R-COO-R\')', icon: <Beaker /> },
        { text: '紅絲絨肯亞 = 磷酸 (H₃PO₄) 的味覺具象化', icon: <Flame /> },
        { text: '目標：建立「控制變因」的品飲對照組', icon: <Microscope /> },
      ]
    },
    {
      id: 'lab-a-1',
      type: 'split',
      section: '實驗模組 A：製程 (Process)',
      title: '水洗 vs. 日曬',
      left: {
        title: '☕ 杯測對照組',
        items: [
          '藝伎組：露西水洗 vs. 露西日曬',
          '衣索比亞組：水洗 TOH vs. 花蝶日曬'
        ]
      },
      right: {
        title: '🧪 科學探究點',
        items: [
          '國小自然：微生物與發酵作用',
          '黏果膠層 (Mucilage) 的代謝',
          '國中理化：滲透壓與物質擴散',
          '乾燥過程中的物質遷移'
        ]
      }
    },
    {
      id: 'lab-b-1',
      type: 'split',
      section: '實驗模組 B：風土 (Terroir)',
      title: '土壤化學與氣候',
      left: {
        title: '☕ 杯測對照組',
        items: [
          '酸質代表：紅絲絨肯亞 (強烈檸檬/烏梅酸)',
          '乾燥代表：葉門摩卡日曬 (香料/陳酒味)'
        ]
      },
      right: {
        title: '🧪 科學探究點',
        items: [
          '化學：有機酸 vs 無機酸 (磷酸 H₃PO₄)',
          '地科：火山土壤的礦物質影響',
          '物理：水活性 (Aw < 0.6) 與保存',
          '極端氣候下的梅納反應路徑'
        ]
      }
    },
    {
      id: 'lab-c-1',
      type: 'split',
      section: '實驗模組 C：發酵 (Fermentation)',
      title: '現代處理法的化學式',
      left: {
        title: '☕ 杯測對照組',
        items: [
          '莫札特 (葡萄乾蜜處理)',
          '豔夏花荔 (雙重厭氧)'
        ]
      },
      right: {
        title: '🧪 科學探究點',
        items: [
          '有機化學：酯化反應 (Esterification)',
          '酸 + 醇 → 酯 (R-COO-R\') + 水',
          '生物：厭氧呼吸作用',
          'C₆H₁₂O₆ → 2C₂H₅OH + 2CO₂ + VOCs'
        ]
      }
    },
    {
      id: 'extraction',
      type: 'card',
      section: '萃取實驗室 (Extraction Lab)',
      title: '變因探究：反應速率與揮發',
      cards: [
        {
          title: '理化老師挑戰：反應速率',
          icon: <Thermometer size={40} />,
          desc: '比較 92°C vs 85°C 水溫',
          science: '高溫 = 高活化能 = 加速溶解，但也可能帶出雜味。低溫保留酸香但可能萃取不足。'
        },
        {
          title: '自然老師挑戰：物質特性',
          icon: <Wind size={40} />,
          desc: '乾香 (Fragrance) vs 濕香 (Aroma)',
          science: '物理擴散作用。觀察研磨後揮發性物質釋放，以及注水後熱氣帶動的分子運動。'
        }
      ]
    },
    {
      id: 'ai-prompt-1',
      type: 'code',
      title: 'AI 課程設計：國中理化篇',
      subtitle: '從感官體驗生成探究教案',
      code: `Prompt 範例：
「我剛品嚐了『莫札特』咖啡，它有強烈的草莓果醬味（酯類香氣）。
請扮演資深理化老師，設計一堂給國二學生的『有機化合物-酯類與發酵』探究課程。

課程要求：
1. 解釋厭氧發酵如何產生特殊的揮發性有機化合物（VOCs）。
2. 設計一個簡單實驗，比較『自然發酵』與『人工添加香精』的嗅覺區別。
3. 結合質量守恆定律，討論烘焙過程中水分散失與重量減輕的關係。」`
    },
    {
      id: 'ai-prompt-2',
      type: 'code',
      title: 'AI 課程設計：國小自然篇',
      subtitle: '從產地觀察連結食農教育',
      code: `Prompt 範例：
「我剛比較了『肯亞水洗豆』與『葉門日曬豆』。
請扮演國小自然科老師，設計一個給高年級學生的『食農教育與植物構造』教案。

課程要求：
1. 利用咖啡果實構造（果皮、果肉、種子）解釋水洗與日曬處理法的不同。
2. 設計觀察活動，透過豆子外觀（皺縮度、銀皮）推測處理法。
3. 加入氣候變遷（如降雨量對乾燥過程的影響）對農業影響的討論。」`
    },
    {
      id: 'cheat-sheet',
      type: 'table',
      title: '講師備課小抄 (Cheat Sheet)',
      rows: [
        { bean: '露西藝伎', science: '酵素作用 / 物質轉化', subject: '生物/化學' },
        { bean: '紅絲絨肯亞', science: 'pH值 / 磷酸 (H₃PO₄)', subject: '化學' },
        { bean: '莫札特', science: '酯類 (Esters) / 揮發性有機物', subject: '有機化學' },
        { bean: '葉門摩卡', science: '水活性 / 乾燥動力學', subject: '地科/物理' },
      ]
    },
    {
      id: 'end',
      type: 'title',
      title: 'Q & A',
      subtitle: '感謝您的參與',
      author: '祝您的課堂充滿咖啡香與科學魂',
      icon: <BookOpen size={64} className="text-emerald-400" />
    }
  ];

  const totalSlides = slides.length;

  const nextSlide = () => {
    if (currentSlide < totalSlides - 1) {
      setCurrentSlide(prev => prev + 1);
      setIsRevealed(false);
    }
  };

  const prevSlide = () => {
    if (currentSlide > 0) {
      setCurrentSlide(prev => prev - 1);
      setIsRevealed(false);
    }
  };

  // Keyboard navigation
  useEffect(() => {
    const handleKeyDown = (e) => {
      if (e.key === 'ArrowRight' || e.key === 'Space') nextSlide();
      if (e.key === 'ArrowLeft') prevSlide();
    };
    window.addEventListener('keydown', handleKeyDown);
    return () => window.removeEventListener('keydown', handleKeyDown);
  }, [currentSlide]);

  const SlideContent = ({ slide }) => {
    switch (slide.type) {
      case 'title':
        return (
          <div className="flex flex-col items-center justify-center h-full text-center space-y-8 animate-fadeIn">
            <div className="p-8 bg-stone-800 rounded-full shadow-2xl border-4 border-amber-600/30">
              {slide.icon}
            </div>
            <h1 className="text-5xl md:text-7xl font-bold text-amber-50 tracking-wider font-serif">
              {slide.title}
            </h1>
            <h2 className="text-2xl md:text-3xl text-amber-200/80 font-light">
              {slide.subtitle}
            </h2>
            <div className="w-24 h-1 bg-gradient-to-r from-transparent via-amber-500 to-transparent my-4"></div>
            <p className="text-xl text-stone-400">{slide.author}</p>
          </div>
        );
      
      case 'list':
        return (
          <div className="flex flex-col h-full justify-center px-4 md:px-16 animate-fadeIn">
            <h2 className="text-4xl md:text-5xl font-bold text-amber-100 mb-12 border-l-8 border-amber-600 pl-6">
              {slide.title}
            </h2>
            <div className="space-y-6">
              {slide.content.map((item, idx) => (
                <div key={idx} className="flex items-center space-x-6 p-6 bg-stone-800/50 rounded-xl hover:bg-stone-800 transition-colors border border-stone-700/50">
                  <div className="text-emerald-400 p-3 bg-stone-900 rounded-lg">
                    {item.icon}
                  </div>
                  <span className="text-xl md:text-2xl text-stone-200">{item.text}</span>
                </div>
              ))}
            </div>
          </div>
        );

      case 'split':
        return (
          <div className="flex flex-col h-full pt-8 px-4 md:px-12 animate-fadeIn">
            <div className="mb-8">
               <span className="text-emerald-400 uppercase tracking-widest text-sm font-bold">{slide.section}</span>
               <h2 className="text-4xl font-bold text-amber-50 mt-2">{slide.title}</h2>
            </div>
            
            <div className="grid grid-cols-1 md:grid-cols-2 gap-8 h-full pb-8">
              {/* Left Side: Coffee */}
              <div className="bg-gradient-to-br from-stone-800 to-stone-900 p-8 rounded-3xl border border-stone-700 shadow-xl flex flex-col justify-center">
                <h3 className="text-2xl font-serif text-amber-400 mb-6 flex items-center">
                  <Coffee className="mr-3" /> {slide.left.title}
                </h3>
                <ul className="space-y-4">
                  {slide.left.items.map((item, idx) => (
                    <li key={idx} className="flex items-start text-lg text-stone-300">
                      <span className="mr-2 text-amber-600">•</span> {item}
                    </li>
                  ))}
                </ul>
              </div>

              {/* Right Side: Science (Reveal Effect) */}
              <div 
                className={`cursor-pointer group relative p-8 rounded-3xl border transition-all duration-500 flex flex-col justify-center overflow-hidden
                  ${isRevealed 
                    ? 'bg-emerald-900/20 border-emerald-500/50' 
                    : 'bg-stone-800 border-stone-700 hover:border-emerald-500/50'}`}
                onClick={() => setIsRevealed(!isRevealed)}
              >
                 {!isRevealed && (
                  <div className="absolute inset-0 flex items-center justify-center bg-stone-800/90 z-10 backdrop-blur-sm transition-opacity">
                    <div className="text-center animate-pulse">
                      <Atom size={48} className="mx-auto text-emerald-400 mb-2" />
                      <p className="text-emerald-200 text-lg font-bold uppercase tracking-widest">點擊揭示科學原理</p>
                    </div>
                  </div>
                )}

                <h3 className="text-2xl font-serif text-emerald-400 mb-6 flex items-center">
                  <Microscope className="mr-3" /> {slide.right.title}
                </h3>
                <ul className="space-y-4">
                  {slide.right.items.map((item, idx) => (
                    <li key={idx} className="flex items-start text-lg text-stone-300">
                      <span className="mr-2 text-emerald-600">→</span> {item}
                    </li>
                  ))}
                </ul>
              </div>
            </div>
          </div>
        );

      case 'card':
        return (
          <div className="flex flex-col h-full pt-8 px-4 md:px-12 animate-fadeIn">
            <div className="mb-8">
               <span className="text-amber-500 uppercase tracking-widest text-sm font-bold">{slide.section}</span>
               <h2 className="text-4xl font-bold text-amber-50 mt-2">{slide.title}</h2>
            </div>
            <div className="grid grid-cols-1 md:grid-cols-2 gap-8 items-center flex-grow pb-12">
              {slide.cards.map((card, idx) => (
                <div key={idx} className="bg-stone-800 p-8 rounded-2xl border border-stone-700 hover:shadow-2xl hover:shadow-amber-900/20 transition-all h-full flex flex-col">
                  <div className="flex items-center mb-4 text-amber-400">
                    {card.icon}
                    <h3 className="text-xl font-bold ml-3">{card.title}</h3>
                  </div>
                  <p className="text-lg text-stone-300 mb-4 font-medium">{card.desc}</p>
                  <div className="mt-auto pt-4 border-t border-stone-700">
                    <p className="text-emerald-300 text-sm leading-relaxed">{card.science}</p>
                  </div>
                </div>
              ))}
            </div>
          </div>
        );

      case 'code':
        return (
          <div className="flex flex-col h-full pt-8 px-4 md:px-16 animate-fadeIn">
             <div className="mb-6 flex items-center">
                <Cpu className="text-purple-400 mr-4" size={32} />
                <div>
                  <h2 className="text-3xl font-bold text-amber-50">{slide.title}</h2>
                  <p className="text-stone-400">{slide.subtitle}</p>
                </div>
             </div>
             <div className="bg-gray-900 p-6 rounded-xl border border-gray-700 overflow-auto shadow-inner flex-grow mb-8 font-mono text-sm md:text-base leading-relaxed text-green-400 relative">
               <div className="absolute top-2 right-4 text-xs text-gray-500 select-none">AI Prompt</div>
               <pre className="whitespace-pre-wrap">{slide.code}</pre>
             </div>
          </div>
        );

      case 'table':
         return (
          <div className="flex flex-col h-full justify-center px-4 md:px-16 animate-fadeIn">
            <h2 className="text-4xl font-bold text-amber-100 mb-8 text-center">{slide.title}</h2>
            <div className="overflow-hidden rounded-xl border border-stone-700 shadow-2xl">
              <table className="w-full text-left border-collapse">
                <thead className="bg-stone-800 text-amber-500">
                  <tr>
                    <th className="p-4 font-bold">豆款名稱</th>
                    <th className="p-4 font-bold">科學原理 (關鍵字)</th>
                    <th className="p-4 font-bold">對應學科</th>
                  </tr>
                </thead>
                <tbody className="divide-y divide-stone-700 bg-stone-900/80">
                  {slide.rows.map((row, idx) => (
                    <tr key={idx} className="hover:bg-stone-800/50 transition-colors text-stone-300">
                      <td className="p-4 font-medium text-amber-100">{row.bean}</td>
                      <td className="p-4">{row.science}</td>
                      <td className="p-4 text-emerald-400">{row.subject}</td>
                    </tr>
                  ))}
                </tbody>
              </table>
            </div>
          </div>
         );

      default:
        return null;
    }
  };

  return (
    <div className="h-screen w-full bg-stone-950 text-stone-200 font-sans overflow-hidden flex flex-col select-none">
      
      {/* Progress Bar */}
      <div className="w-full h-1 bg-stone-900">
        <div 
          className="h-full bg-gradient-to-r from-amber-600 to-emerald-500 transition-all duration-300 ease-out"
          style={{ width: `${((currentSlide + 1) / totalSlides) * 100}%` }}
        />
      </div>

      {/* Main Content Area */}
      <div className="flex-grow relative w-full max-w-6xl mx-auto py-6">
        <SlideContent slide={slides[currentSlide]} />
      </div>

      {/* Navigation Footer */}
      <div className="h-16 border-t border-stone-800 bg-stone-900/50 backdrop-blur flex items-center justify-between px-6 z-20">
        <div className="flex items-center space-x-2 text-sm text-stone-500 font-mono">
          <Menu size={16} />
          <span>{currentSlide + 1} / {totalSlides}</span>
        </div>

        <div className="flex space-x-4">
          <button 
            onClick={prevSlide}
            disabled={currentSlide === 0}
            className={`p-2 rounded-full transition-colors ${currentSlide === 0 ? 'text-stone-700 cursor-not-allowed' : 'text-amber-500 hover:bg-stone-800 hover:text-amber-400'}`}
          >
            <ChevronLeft size={28} />
          </button>
          <button 
            onClick={nextSlide}
            disabled={currentSlide === totalSlides - 1}
            className={`p-2 rounded-full transition-colors ${currentSlide === totalSlides - 1 ? 'text-stone-700 cursor-not-allowed' : 'text-amber-500 hover:bg-stone-800 hover:text-amber-400'}`}
          >
            <ChevronRight size={28} />
          </button>
        </div>
      </div>

      <style jsx global>{`
        @keyframes fadeIn {
          from { opacity: 0; transform: translateY(10px); }
          to { opacity: 1; transform: translateY(0); }
        }
        .animate-fadeIn {
          animation: fadeIn 0.5s ease-out forwards;
        }
      `}</style>
    </div>
  );
};

export default Presentation;
```


## 🖼️ 相關參考圖片與文件
![[10093408293917864137-4c73aabdd108e0ae.png]] ![[Gemini_Generated_Image_8ggri88ggr-44a8442c4e958641.jpg]] ![[Gemini_Generated_Image_htsu2ahtsu-e11f6500ada5b266.jpg]] ![[Gemini_Generated_Image_k0hcak0hca-96a4cef5cc1008b1.jpg]] ![[Gemini_Generated_Image_k0hcak0hca-be202b15b4d41e70.jpg]] ![[Gemini_Generated_Image_q59snqq59s-d7376f45389320a6.jpg]] ![[Gemini_Generated_Image_ykv67oykv6-153057063c8c793c.jpg]] ![[Gemini_Generated_Image_ykv67oykv6-758609cfa28db9a7.jpg]]

## 🔬 科學物理觀點解析
- *此理論卡片由 Gemini Takeout 匯出對話分析自動生成。*

## 🔗 相關理論與對話推薦
- [[2026-03-26_請同時給我這一支豆子在同一台機器上詳細的烘焙計畫_800]] (共用特徵: `莫札特, 藝伎, 肯亞`)
- [[2026-01-22_咖啡深夜食堂研習簡報_1519]] (共用特徵: `莫札特, 藝伎, 肯亞`)
- [[2026-01-24_這是一份為您的「咖啡深夜食堂」研習設計的專屬菜單。這份菜單將居酒屋的「手寫木牌」風格與「理化教科書」的硬核知識結合，旨在_1498]] (共用特徵: `莫札特, 藝伎, 肯亞`)
- [[2026-02-24_如果要烘像藝伎這種昂貴的豆子，需要如何兼顧烘出花香和果酸的淺焙豆，但又要DTR15%以上，之前在小台tank_200烘莫_1196]] (共用特徵: `莫札特, 藝伎, 厭氧`)
- [[2026-02-24_沒有看到圖_1192]] (共用特徵: `莫札特, 藝伎, 厭氧`)

---
name: engineer
description: 讀取 input/requirements.txt 一鍵產出/更新 YAML 行為指令
---

# /engineer

請套用 `prompt_architect` Skill，讀取 `input/requirements.txt`，分析對話機器人指令需求與版本疊加，將產出的完整 YAML 指令寫入 `output/system_instructions.yaml`。
